# Offline image archive

`mihomo-v1.19.27-linux-amd64.tar` is a loadable `linux/amd64` image archive
tagged as `metacubex/mihomo:v1.19.27`.

SHA-256:

```text
768a93f1a39fe62d7479db85bb1f9925e245dba9fc4d8d8d46bd4a799fccc4ff
```

On an x86_64 Linux server, run `../load-image.sh` from this directory's parent
after placing a configured `clash.yaml` there.
